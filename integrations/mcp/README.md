# Agents IA / MCP

Créer une clé API Premium puis configurer le client MCP avec l'URL `https://<hôte>/mcp` et l'en-tête `Authorization: Bearer <clé>`.

Le serveur cible MCP 2026-07-28 ; un client ancien limité au handshake `initialize` 2025-06-18 devra être mis à jour ou utiliser l'API REST.
