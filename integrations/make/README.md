# Make

Utiliser le module **HTTP > Make a request** avec une clé API BD Desk. Pour les scénarios événementiels, créer un **Custom webhook** Make puis l'ajouter dans BD Desk.

Le même contrat REST que n8n est utilisé : aucune logique métier spécifique à Make n'est nécessaire.
