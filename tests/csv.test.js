import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
import {parseDelimited,parseBdgestCsv,inspectBdgestCsv,toIsoDate} from '../src/csv.js';
test('parse delimiter quoted',()=>assert.deepEqual(parseDelimited('a;b\n"x;y";z\n'),[['a','b'],['x;y','z']]));
test('parse BDGest real export',()=>{const rows=parseBdgestCsv(fs.readFileSync(new URL('./fixtures/bdgest-sample.csv',import.meta.url),'utf8'));assert.equal(rows.length,4);assert.equal(rows.filter(x=>x.isbn).length,3);assert.equal(rows.filter(x=>x.firstEdition).length,2);assert.equal(rows.filter(x=>x.read).length,2);assert.equal(rows[0].title,'Premier')});
test('inspecte un export BDGest avant écriture',()=>{const csv=fs.readFileSync(new URL('./fixtures/bdgest-sample.csv',import.meta.url),'utf8');const preview=inspectBdgestCsv(csv);assert.equal(preview.valid,true);assert.equal(preview.rows,4);assert.equal(preview.missingHeaders.length,0);assert.equal(preview.invalidRows,0);assert.equal(preview.isbnPresent,3);assert.equal(preview.duplicateIsbnGroups,1)});
test('inspecte et rejette un CSV qui ne décrit pas BDGest',()=>{const preview=inspectBdgestCsv('Titre;Auteur\nAlbum;Auteur');assert.equal(preview.valid,false);assert.deepEqual(preview.missingHeaders,['Table','IdAlbum']);assert.ok(preview.errors.length>0)});
test('dates FR vers ISO',()=>{assert.equal(toIsoDate('14/06/2023'),'2023-06-14');assert.equal(toIsoDate(''),null)});
